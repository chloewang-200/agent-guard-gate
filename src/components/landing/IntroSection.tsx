/**
 * Placeholder section — `Index` expects this module; extend with real intro copy when ready.
 */
export function IntroSection() {
  return <section className="py-8 md:py-12" aria-label="Introduction" />;
}
